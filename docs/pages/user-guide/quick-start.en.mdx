import { Tab, Tabs } from "nextra-theme-docs";
import { Steps } from "nextra/components";

# Quick Start

## Get CPLib from Snapshot

CPLib is a library that consists only of header files. This means you don't have to worry about linking issues. The recommended way to use CPLib is to include the preprocessed single-header file. You can find the latest snapshot of the single-header file based on the main branch on [GitHub](https://github.com/rindag-devs/cplib/blob/single-header-snapshot/cplib.hpp).

Alternatively, you can download CPLib to a local folder using terminal commands.

<Tabs items={['UNIX-like', 'Windows']} storageKey="selected-os">
<Tab>
Open the terminal and enter the following command:

```bash
curl -o cplib.hpp -L https://github.com/rindag-devs/cplib/raw/single-header-snapshot/cplib.hpp
```

</Tab>
<Tab>
Launch PowerShell and enter the following command:

```powershell
Invoke-WebRequest -Uri "https://github.com/rindag-devs/cplib/raw/single-header-snapshot/cplib.hpp" -OutFile "cplib.hpp"
```

</Tab>
</Tabs>

This will download the latest snapshot of the single-header file based on the main branch and save it as `cplib.hpp` in the current directory.

### For Older Compilers (without `<format>`)

If you are using an older compiler (e.g., GCC < 13) that does not support the `<format>` header, you can use the `with_fmt` version of the single-header file. This version embeds the [fmt](https://github.com/fmtlib/fmt) library to provide formatting support.

You can download it from:
[https://github.com/rindag-devs/cplib/raw/single-header-snapshot/with_fmt/cplib.hpp](https://github.com/rindag-devs/cplib/raw/single-header-snapshot/with_fmt/cplib.hpp)

## Build from Source

In addition to using snapshots directly, building from source is another way to obtain a single file header.

<Steps>
### Clone Git Repo

```bash
git clone https://github.com/rindag-devs/cplib.git --depth=1 --single-branch
cd cplib
```

### Build Single Header File

```bash
cmake -B build
cmake --build build --target single_header
```

### Get Output File

The final code is stored in `build/cplib.hpp`.

### Build with Embedded fmt

If you need to embed the `fmt` library for older compilers:

```bash
cmake -B build -DCPLIB_USE_FMT_LIB=ON
cmake --build build --target single_header
```

</Steps>

## Start Learning

This document will explain how to use CPLib with examples. Additionally, whether you have prior experience using libraries like Testlib for development or not, it is recommended to start reading from the [Checker](/user-guide/basics/checker) chapter.
